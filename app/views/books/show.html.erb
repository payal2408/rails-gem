<h1><%= @book.title %></h1>

<p><%= @book.body %></p>
<ul>
  <li><%= link_to "Edit", edit_book_path(@book) %></li>
  <li><%= link_to "Destroy", book_path(@book),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %></li>
</ul>